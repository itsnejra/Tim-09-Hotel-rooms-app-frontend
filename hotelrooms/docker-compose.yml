version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: frontend_app
    container_name: frontend_cont
    ports:
      - "3000:3000"
    volumes:
      - .:/usr/src/app
